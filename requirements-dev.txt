# Test-only deps. At runtime the host (protoAgent) provides langchain-core, fastapi,
# and pyyaml; these let the suite run standalone in CI with no host present.
fastapi>=0.110
langchain-core>=0.2
pyyaml>=6
httpx>=0.27
pytest>=8
